``dmx.torch_stats`` API
=======================

.. automodule:: dmx.torch_stats

This optional backend requires PyTorch. Its models use tensor-specific
protocols and device semantics; see :doc:`/installation` for installation
options rather than treating the NumPy and torch backends as interchangeable.

.. toctree::
   :maxdepth: 1

   protocols
   distributions
   structured_models
   latent_models
